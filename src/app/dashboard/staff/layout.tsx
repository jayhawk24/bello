import { pageMetadata } from "@/lib/seo";

export const metadata = pageMetadata.dashboardStaff;

export default function StaffLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
