import { pageMetadata } from "@/lib/seo";

export const metadata = pageMetadata.dashboardHotel;

export default function HotelLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
