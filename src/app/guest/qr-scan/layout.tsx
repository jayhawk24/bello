import { pageMetadata } from "@/lib/seo";

export const metadata = pageMetadata.guestQrScan;

export default function GuestQrScanLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
